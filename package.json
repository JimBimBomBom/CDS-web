{
  "name": "cds-frontend",
  "version": "1.0.0",
  "description": "City Distance Service frontend + Playwright E2E tests",
  "scripts": {
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:debug": "playwright test --debug"
  },
  "devDependencies": {
    "@playwright/test": "^1.58.2"
  }
}
